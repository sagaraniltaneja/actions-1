name: My First Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome message
              uses: actions/checkout@v4

            - name: Welcome message
              run: echo "My first GitHub Action demo"
            
            - name: List files on repository
              run: ls

            - name: Read file
              run: cat README.md